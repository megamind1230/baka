* TODO ideas


** others
*** make a wish
1. [ ] idea `make a wish` or `wish dot com` website { should be education-wise centric }
   - sign in > make a wish {name something+put a price for it}
   - wait in queue
   - get blessed by ppl donating to any of your donation apps { vodafone cash / instapay / fawry / paypal / bank account .. etc }



4. [ ] rank / vote per face { the zuckerberg website from the movie }
   - a tender like website { refreshed weekly }
   - every account must be authed
   - people rate a person with 3 buttons { nice 👍 / idk ❓ / bad 👎 }
   - also has this mode
   - rank your best friends or best friend leaderboard app {like the one in the facebook movie}
   - you create a community.. add image of each person
   - ppl signing in can pick between 1 of 2 friends showing
   - you can comment the reasons for your pick
   - small comments about 100 words max

5. [ ] a website for ranking codeforces problems 
   - and having some discussion about them and the best resource to solve them from .. not giving direct solutions tho
   - and giving `time outs / bans` to cheaters / ruiners / spoilers
   - website must be moderated / have a review team


7. [ ] idea website learn this trick today
   - a database of tricks in games like league of legends and dota2 like eg 
   - you sign up with email/rss feed / telegram / whatsapp to get emails + browser/mobile notifications 
   - determine game > determine champ/hero 
   - now every day you learn 1 tricks macro and micro 

8. [ ] idea website find a friend
   - you sign in
   - 2 levels of authentecity
     - 1st is to give your social media websites available at least 2
     - 2nd give and real images of you { not AI } over the course of 3 days .. every day you send 1
   - the website ranks the tags by popularity {by default}
   - nudity/harrassment detected is perma banned
   - unbanning requires a huge fine tax
   - you see one person at a time with his tags
   - you answer by yes or no
   - or simply you just pick that specific tag that you both have in common
   - the website keeps filtering ppl and showing more tags to try and provide you with the best pool of ppl from which you can find a friend
   - you can view each user profile as a page of his social media platforms

9. [ ] we need a simple communication application
   - the website is a txt file and domain generator
   - sign up with username .. get into dashboard of all communication files .. you can create txt files
   - each file is has its link as follows {username1-username2}
     - if hope i can make the {username2-username1} redirect to same page
   - page UI is  split into 2 panes .. for each user
     - each time the user inputs a message .. it leaves a timestamp before the message
   - only txt website
   - files are volatile .. deleted every day
   - maybe later .. we can add dump channels ... like forums {with question in the link}
** dota 2

- [ ] create a modular dota 2 website that has this mode
  1. [ ] code to visualize the holy trifecta {dota2/league}
     - dota2 purge trading ep 3
     - can try provide a csv of values .. advanced i guess
     - or at least tells me to either
     - kite, attack while in range , run down enemy, run from enemy 
  2. [ ] create a dota2 synergy app
     - you have a database with each hero having descrptive tags
     - then you query the database according to synergy
     - ex: carry lacks cc > give him supp with cc recommendation
  3. [ ] idea .. simple drafting website for league and dota2 {with numbers representing how good a champ is}

  [2026-05-07 Thu 22:59]





* WAITING heatmapstreak.com
** phase 1
+ heatmapstreak.com : a website where you
  - sign in/log in { remember your data }
  - add as main habit pages as you want
  - it shows the Year in a heatmap fashion
  - pressing on a day toggles its state light it up/turns it off { and it remembers them all for all of your habits }
  - each account has unlimited amount of habits
  - each habit has its own tab or whatever best fits the design
  - each habit can have its own color
+ maybe we can make a dashboard where user can see another heatmap showing how many habbits did this user toggle
  - the more habits he toggled on that day, the darker it gets

+ [X] Streak counter
   Show current longest streak for each habit (consecutive days toggled on)
  
** phase 2
+ habits optionally can be either having a goal number of days to complete {for example 21 days}, or a perdiocally repeated goals {3 per week or whatever}

+ [ ] highlight the current day cell 

+ [ ] Weekly/monthly stats
   Completion rate graphs, averages per week/month, best/worst days
    
+ [-] Dark mode
   Toggle between light and dark themes
    
+ [ ] Notes per day
   Attach a small text note to each day for a habit
    
+ [ ] Reminders/notifications
   Optional daily reminder to check in (email or push)
    
+ [ ] Export data
   Download habit data as CSV/JSON




* TimeBlock

* the coding sloth ideas and more

1. Calculator
   but it a websites-launcher depending on the result {ex: 100=google.com, 101= youtube , etc }
2. Portfolio 
3. To do list
4. personal Finance tracker
5. Real time chat
6. Make own redis
7. Random quote generator 
8. Algorithm visualizer 
9. Travel booking system 
10. Quiz program 
11. Make own bittorrent
12. HTTP Server
13. Real time editor 
14. Chat bot
15. Qr code generator
16. AI gf/bf
17. Make own git
18. Neural network 
19. Video game

file organizer
quiz app
student grading system
notes app
timer
music player UI
expense tracker
weather app
screenshot organizer

Beginner:
- Book/Movie Catalog — CRUD with Entity Framework, search/filter, validation
- Blog Engine — Posts, comments, categories; auth middleware, custom middleware for logging
Intermediate:
- URL Shortener — Redirect middleware, custom route constraints, DI for services
- Task/Todo Manager — Razor Pages UI, dependency-injected repository, middleware for request timing
- E-Commerce Cart — Sessions, cookie middleware, Razor partials for product cards
More Advanced:
- Real-time Dashboard — Chart.js + Razor, custom status-code middleware, DI for background services
- Multi-tenant SaaS — Tenant-resolving middleware, scoped DI per request, shared layout with sections

1. Recipe Manager — CRUD with categories, ingredients, search
 2. Expense Tracker — Pie/bar charts, monthly filtering, categories
 3. Event/Meetup RSVP — Event creation + attendee signup with email notification
 4. Flashcard Quiz App — Decks, spaced repetition, session tracking
 5. Inventory System — Stock in/out, low-stock alerts, supplier list
 6. Job Board — Job listings, apply form, company profiles
 7. Issue/Bug Tracker — Status workflow, comments, assignee filter
 8. Habit Tracker — Daily check-in, streaks, heatmap calendar
 9. Voting/Poll App — Create polls, real-time results, share links
10. Code Snippet Repository — Syntax-highlighted snippets with tags
11. Movie/Show Watchlist — Track watched/to-watch, ratings, reviews
12. Invoice Generator — Line items, totals, PDF export
13. Fitness Log — Exercises, sets/reps, progress graphs
14. Flight/Hotel Booking — Search, availability calendar, booking flow
15. Chat Application — SignalR real-time messaging, rooms, user list
16. Kanban Board — Drag-and-drop columns, cards, assignees
17. Link Collection / Bookmarks — Folders, tags, click tracking
18. API Wrapper Dashboard — Fetch external APIs, cache, display data
19. Flash Sale / Coupon System — Time-limited deals, code generation
20. Gym Class Scheduler — Time slots, instructor assignment, capacity limit

    Here are 10 ideas, each mapping to why auth fits naturally:
League of Legends
1. Build Sharer — Upload/favorite rune builds. Auth: creator ownship, Admins moderate, "Pro verified" claim.
2. Tournament Ladder — Manage teams, register for events. Auth: TeamCaptain role, MatchAdmin claim. OAuth to sign up via Google.
3. Champion Matchup Tracker — Track winrates per champion. Auth: free tier (view) vs. premium (unlimited data). Claims-based feature gating.
Obsidian / Note-taking
4. Shared Vault API — Sync notes between devices via REST API. Auth: user owns notes, CanShare claim allows public links, OIDC for Google login.
Dota 2 (same genre, different flavor)
5. Hero Counter-Picker — Suggest counters based on enemy draft, upvote/downvote strategies. Auth: Authorize to submit, Moderator role to approve, CanEdit claim.
Linux
6. Linux Config Backup API — Store dotfiles/backups with versioning. Auth: user scoped (see your backups only), Admin role for server-wide view.
7. Sudo History Viewer — Teams log sudo commands to audit. Auth: CanViewAll claim vs. CanViewOwn claim. Role-based for Ops vs. Dev.
Languages
 8. Flashcard API with Spaced Repetition — Per-user decks + scheduled reviews. Auth: user isolation (can't see others' cards), Premium claim unlocks unlimited decks.
 9. Translation Suggestion Board — Crowdsource translations, vote best ones. Auth: CanTranslate claim, Reviewer role approves entries. OIDC to verify identity.
10. Language Buddy Matcher — Match learners with native speakers. Auth: profile ownership, Verified claim after email/OIDC confirmation, Admin role for reports.

** Beginner
*** Library Management System
Services:

#+begin_src text
Book Service
User Service
Borrow Service
#+end_src

Use:

- ASP.NET Core Web API
- Docker


*** Student Management System
Services:

#+begin_src text
Student Service
Course Service
Enrollment Service
#+end_src

Use:

- SQL Server
- JWT


** Intermediate
*** Food Delivery System
Services:

#+begin_src text
Restaurant Service
Order Service
Payment Service
Notification Service
#+end_src

Use:

- Ocelot
- RabbitMQ
- Docker


*** Online Store
Services:

#+begin_src text
Auth Service
Product Service
Cart Service
Order Service
Payment Service
#+end_src

Use:

- JWT
- RabbitMQ
- gRPC


** Advanced (Excellent Portfolio Project)
*** Uber-like Ride Booking
Services:

#+begin_src text
Auth Service
Driver Service
Passenger Service
Ride Service
Payment Service
Notification Service
Location Service
#+end_src

Features:

- JWT Authentication
- RabbitMQ events
- gRPC communication
- Docker
- Kubernetes
- API Gateway

This project touches nearly every objective in the task and looks strong on a .NET backend portfolio.

* TODO create an on-screen-map clicker for linux 
  [2026-05-29 Fri 17:30]

a hoverover screen of squares each having a number that you can click on by typing that number on keyboard


* Real Beginner Project Ideas for asp.net
- Todo app
- Student management system
- Notes app
- Blog system
- Authentication app
- Expense tracker
- E-commerce site
- LMS
- Admin dashboard

* Most Important Concepts to Truly Understand asp.net
These matter more than memorizing syntax:
- Request/response lifecycle
- Routing
- MVC separation
- Dependency Injection
- Middleware pipeline
- Model binding
- Razor rendering


* Typical Learning Path of asp.net
A good order:

1. Create MVC app
2. Learn routing
3. Controllers
4. Views + Razor
5. Models
6. Forms
7. Validation
8. Entity Framework Core
9. Authentication
10. APIs


* an mcq/quizz creator website
a website that create mcq/quizz from textbooks/pdfs

* an ascii image converter
convert images into a simple colored ascii of characters

* imagePocket app
can save screenshots or copied images into this pocket

then you can use them into other apps

* a markdown to mindmap and the other way around app

plus add the functionality to (enter = same lvl sibling , tab = down lvl child = shift tab = up lvl parent)

it would have a dynamic rendering
* WAITING we need a
[-] idea.. `we need a` or `WeNeedA` .. app to save number of ppl in a region according to their profession

+ simple website { more database centric }
+ list by country + governorate + city + village + craft/job/transportation
+ the website lists people according to these filters
+ each item with their social media / phone number contacts + a simple avator
+ `can add rating for ppl too later on` but this requires signing in and auth from both sides

----------------------
+ dotnet cross platform + sqlite
+ 3 levels/ways to add data: 1st by send data to the admin account for him to add it to the system using his admin accound, 2nd from verified people on the website { people who verified their emails and identity using their SSID } : those people have their accounts push data into the /verified domain, 3rd finally people who only just made their account { but still didn't verify it } their data send are shown on the /notverified domain
+ make it english only first

----------------------
+ asp.net with razor pages
+ 4 levels { country - governorate - city - village }
+ admin first creates some primitive tags for jobs etc... but verified accounts can add new tags {which also the admin can later edit}
+ ###### [X] - Admin account: admin@weneeda.com / Admin@123

----------------------
- [X] what is Slug in attributes? { is it an abbreviation?}
  - Country - Id, Name, Slug, CreatedAt
  - Governorate - Id, Name, Slug, CountryId, CreatedAt
  - City - Id, Name, Slug, GovernorateId, CreatedAt
  - Village - Id, Name, Slug, CityId, CreatedAt
----------------------
- [ ] admin can delete any profile/entry
  meanwhile verified users can only delete their /verified or /notverified content/entry/profil
- can we make attributes simpler and less {for example: why use CreatedAt } { or at least explain why using all of these }
- [ ] the app must be fast and flashy .. cuz people are using it for quick people filtering and search for an emergent profession
- [ ] each user can add up to 3 phone numbers
- [ ] once the user is verified .. all of his entries/ profiles are moved from /notverified to /verified
- [ ] can this website handle 1 million visits per day ?
- [ ] can we add a way so that tags are in a txt file for example { just to make it easier to seed tags into the project }, and each newly added tags are projected into that same file... instead of hard-coding the seeds
- [ ] make a donation page


* idea make Chineasy pdf for every Chinese character with its drawing

how about a static website too
* make this app offline
[tldraw • very good free whiteboard](https://www.tldraw.com/?utm_source=chatgpt.com)



* more ideas
Here are some solid microservice project ideas that map well to the task requirements (API Gateway, Service Discovery, gRPC/RabbitMQ, Docker/K8s):
1. E-Commerce Platform (classic, well-understood)
Microservices: User, Product, Order, Payment, Notification
- gRPC: Order → Payment (synchronous, need response)
- RabbitMQ: Order → Notification (async, fire-and-forget emails/SMS)
- Cleanest mapping to the TIL material
2. Food Delivery System (modern, engaging)
Microservices: Restaurant, Menu, Order, Delivery, Notification
- gRPC: Order → Restaurant for availability check; real-time delivery tracking
- RabbitMQ: Order placed → Notification + analytics
- Naturally demonstrates both sync and async patterns
3. Booking/Reservation System (clear sync/async split)
Microservices: User, Venue, Booking, Payment, Notification
- gRPC: Booking checks venue availability synchronously
- RabbitMQ: Booking confirmed → Payment + notification events
4. Online Banking / Payment System (data consistency focus)
Microservices: Account, Transaction, FraudDetection, Notification, Loan
- gRPC: Transaction → FraudDetection (needs immediate validation)
- RabbitMQ: Transaction → Notification
My recommendation: the E-Commerce Platform is the safest choice — the TIL already sketches its architecture, and every objective (gateway, discovery, gRPC, RabbitMQ, Docker, K8s) has a natural place. The Food Delivery system is the most fun if you want something different.
Which direction feels right to you?
▣  Plan · Big Pickle · 19.1s
how about ideas from the things i like
i like gaming (dota2, league of legends)
i like linux (arch, scripting, twm vs de)
i like notes (obsidian, neovim, emacs, org mode)
Thought: 7.7s
The user is sharing their interests and wants microservice project ideas that tie into what they like:

1. Gaming (Dota 2, League of Legends)
2. Linux (Arch, scripting, TWM vs DE)
3. Notes (Obsidian, Neovim, Emacs, Org mode)

Let me think of microservice ideas that combine these interests with the .NET microservices architecture requirements.

Gaming themed ideas:
- Matchmaking stats tracker (Dota 2 / League)
- Game build/recommendation service
- Gaming community platform

Linux themed ideas:
- Distro comparison service
- Arch package tracker
- Dotfile management service
- TWM/DE benchmarking

Notes themed ideas:
- Note-taking sync service (like a mini Obsidian sync)
- Org-mode collaboration service
- Note search/indexing service

Let me think about which ones would best demonstrate all the architecture requirements (API Gateway, Service Discovery, gRPC, RabbitMQ, Docker, Kubernetes).

Idea: Game Stats Aggregator / Match Analysis Platform (gaming focus)
- Services: User (profiles), Match (game data), Analysis (stat calculations), Notification (updates)
- gRPC: Match → Analysis (real-time stat computation)
- RabbitMQ: New match data → Notification (new highlights available)
- API Gateway: Ocelot routing
- Docker + K8s deployment

Idea: Note Sync & Collaboration Service (notes focus)
- Services: Auth, Notes, Sync, Search, Notification
- gRPC: Note → Search (real-time indexing)
- RabbitMQ: Note update → Sync (propagate to collaborators)
- API Gateway, Docker, K8s

Idea: Linux System Dashboard / Dotfile Manager (Linux focus)
- Services: User, Config (dotfiles), Package (script tracking), Analytics, Notification
- gRPC: Config validation checks
- RabbitMQ: Config changes → backup/notification

Idea: Gaming Build & Strategy Platform (gaming focus, very practical)
- Services: User, Hero (character data), Build (recommendations), Match (history), Notification
- gRPC: Build → Match stats for recommendation
- RabbitMQ: New meta analysis → Notification

Idea: Esports Tournament Platform (gaming)
- Services: Team, Match, TournamentBracket, Stream, Notification
- Good for demonstrating complex service interactions

Idea: Org-mode / Obsidian Plugin Ecosystem with Cloud Sync (notes + gaming)
A note-taking platform where:
- Services: Auth, Notes, Tags, Analytics, Notification
- Could also integrate gaming notes/wiki

Let me present these in a structured way that ties back to the technical requirements.
Great tie-in ideas! Here are microservice project concepts tailored to your interests:
🎮 Gaming: Match Analysis & Build Recommender
e.g., "MetaMancer" — analyze your Dota/League matches
Service
Hero
Match
Analysis
Notification
- gRPC: Match → Analysis (compute stats on demand)
- RabbitMQ: Match imported → Analysis recalculates → Notification fires
📝 Notes: Cloud Sync & Search Engine
e.g., "OrgFlow" — sync & search across Obsidian/Neovim/Emacs
Service
Notes
Search
Sync
Hook
- gRPC: Notes → Search (index immediately)
- RabbitMQ: Note edit → Sync propagates to other clients
- Perfect chance to write an Obsidian plugin and Neovim client
🐧 Linux: Dotfile Manager & System Dashboard
e.g., "DotHub" — manage, version, and audit dotfiles + TWM configs
Service
Config
Template
Audit
News
- gRPC: Config → Audit (validate on upload)
- RabbitMQ: Config change → deploy action to agents on machines
- Could write an Arch PKGBUILD for the agent side
🎮📝 Hybrid: Gaming Notes / Strategy Wiki
e.g., "GameBook" — collaborative hero guides, build notes, patch notes tracker
Service
Guide
Patch
Build
Feed
- gRPC: Guide → Build (pull real-time stats)
- RabbitMQ: Patch released → Feed dispatches alerts
My top picks for your personality:
1. MetaMancer (gaming) — you'll care about the data, and pulling real match data makes it feel alive
2. OrgFlow (notes) — directly useful for your own workflow, and you can dogfood it
3. GameBook (both) — best of both worlds if you want something unique
Want me to flesh out one of these into a full architecture plan with service boundaries, communication flows, and deployment design?
▣  Plan · Big Pickle · 19.2s
more ideas
Thought: 768ms
The user wants more ideas for microservices projects based on their interests. Let me come up with more creative ideas that tie into gaming (Dota 2, League of Legends), Linux (Arch, scripting, TWM vs DE), and note-taking (Obsidian, Neovim, Emacs, org-mode).
Sure, here are more niche ideas that hit your interests from different angles:
🎮 Dota 2 / League Draft Counter-Picker
"DraftWise" — a microservice that suggests counter-picks in real time
Service
Hero
Counter
Draft
Meta
Replay
- gRPC: Draft → Counter (instant suggestion during ban phase)
- RabbitMQ: Match ends → Replay queued → Meta updated
- Fun hook: Discord bot that pulls draft suggestions
🐧 Arch Linux AUR Health & Dependency Watchdog
"AURWatch" — keep your AUR packages healthy
Service
PkgWatch
DepGraph
Alert
Script
- gRPC: PkgWatch → DepGraph (resolve deps on demand)
- RabbitMQ: Upstream release → Alert → optional auto-rebuild
- Could generate a personal Arch repo from your watched packages
📝 Obsidian → Emacs Org-mode Bidirectional Sync Engine
"BiSync" — keep your notes in both worlds
Service
Parse
Link
Conflict
Webhook
Graph
- gRPC: Parse → Link (resolve cross-links after conversion)
- RabbitMQ: Note changed → Conflict checks → Webhook pushes to other tool
- You'd write an Obsidian plugin + Neovim plugin + Emacs package
🎮 Personal Gaming Stats Aggregator
"Grimstroke" — your personal performance dashboard across games
Service
Ingest
Profile
Analysis
Dashboard
Goal
- gRPC: Ingest → Analysis (on-demand deep dive)
- RabbitMQ: New match → Ingest queues → Profile updates → optional Goal progress check
- Personal data is private — you own the whole pipeline
🐧📝 TWM Config Generator with Org-mode Documentation
"TWMStudio" — generate and document your i3/hyprland/awesome config
Service	Role
Template	Keybinding templates per TWM (i3, Hyprland, Awesome, Qtile)
Generator	Merges your preferences into working config
DocGen	Generates Org-mode reference doc from your keybinds
Gallery	Share/screenshot community configs
Hook	Applies config, reloads TWM remotely
- gRPC: Generator → DocGen (generate docs on config build)
- RabbitMQ: Config published → Hook applies to registered machines
- Write the client as a Go or Rust binary for your Arch system
🎮🐧 Linux Gaming Benchmark Pipeline
"FrameMiner" — benchmark logging & analysis for Linux native/Proton games
Service
Capture
Bench
Compare
Report
Tweak
- gRPC: Capture → Compare (diff against last 3 runs)
- RabbitMQ: Benchmark submitted → Report generated → Alert if regression
- Pair it with a MangoApp config that posts to your API
📝🎮 Strategy Guide Wiki (with Org-mode Export)
"GuideCraft" — write guides in Org-mode, publish everywhere
Service
Source
Render
Hero
Patch
Review
- gRPC: Source → Render (preview on save)
- RabbitMQ: New patch → Patch marks affected guides → Review notifies authors
- You write guides in Emacs Org-mode; the system publishes them
🐧 Dotfile "Nix-lite" — Bash Scripted Environment Manager
"EnvLite" — manage environments declaratively without Nix
Service
Declare
Build
Check
Sync
Log
- gRPC: Build → Check (validate before applying)
- RabbitMQ: Sync triggered → pushes to all registered machines
- Everything is shell-scriptable; no complex DSL to learn
Want me to pick one and build a full architecture plan (service boundaries, proto files, DB schemas, API Gateway routes, RabbitMQ exchanges, Docker Compose layout, K8s manifests)?
▣  Plan · Big Pickle · 21.9s
more
Thought: 345ms
The user wants more ideas. Let me keep going with creative intersections of their interests.
A few more that blend your interests in unusual ways:
🎮 Dota 2 Replay Parser + Org-mode Match Journal
"PostGame" — auto-generate a match journal in Org-mode after every game
Service	Role
Parser	Parses .dem replays, extracts timeline (wards, runes, fights, deaths)
Journal	Generates structured Org-mode notes with timestamps, tables
Stats	Computes GPM/XPM graphs, last-hit comparisons
Tag	Lets you tag games ("throw", "clutch", "new build test")
Search	Query past games by hero, bracket, outcome, tags
- gRPC: Parser → Stats (compute advanced metrics synchronously)
- RabbitMQ: Match finished → Parser queued → Journal generated → Notification
Think of it like: every game you play → Emacs Org-agenda gets a new entry with your build, KDA, and a "what I learned" section you fill in.
🐧📝 Neovim Plugin Registry + Build Pipeline
"VimForge" — discover, build, and keep Neovim plugins updated
Service
Catalog
Build
Doc
Pack
Health
- gRPC: Catalog → Build (trigger build on demand)
- RabbitMQ: Plugin updated on GitHub → Build queues → Pack updates spec
You'd host this as your personal plugin "curated registry" for the ones you actually use.
🐧 Tiling Window Manager Session Manager
"TileState" — save and restore complete TWM sessions
Service	Role
Layout	Captures window tree, geometry, tags/workspaces
App	Knows which apps you had open (terminal in ~/dev, browser on wiki)
Session	Save/load named sessions ("coding", "gaming", "writing")
Restore	Reopens apps, repositions, restores tmux sessions
Hook	Integrates with i3 IPC, Hyprland socket, Awesome DBus
- gRPC: Session → Restore (restore complex layouts)
- RabbitMQ: Screen lock/TWM reload → queued auto-save
Imagine: tileset save coding → captures neovim + terminal + browser layout. tileset load coding → everything back exactly where it was.
🎮📝 Patch Note Differ (Org-mode Edition)
"DiffThePatch" — turns verbose game patch notes into clean Org diffs
Service
Scrape
Parse
Diff
Export
Digest
- gRPC: Parse → Diff (compute stat changes on demand)
- RabbitMQ: New patch → Scrape fetches → Parse queues → Digest generates → posted to your blog/notes
You could publish this as an Org static site for your guild/friends.
🐧🎮 ProtonDB + Wine Triage Dashboard
"ProtonPulse" — track your personal game compatibility on Linux
Service
Library
Triage
Workaround
Regress
Report
- gRPC: Triage → Workaround (apply known fixes before first run)
- RabbitMQ: Proton updated → Regress checks your library → Alert on breakage
Every time you run a game, a one-shot script reports the result + proton version + flags back to the dashboard.
🐧📝 Org-mode / Neovim Daily Template Engine
"JournalCraft" — structured daily journals from your tools
Service
Template
Capture
Rollup
Goal
Export
- gRPC: Capture → Template (render entry into your preferred Org structure)
- RabbitMQ: Day ends → Rollup queues → Goal progress computed
Every morning your ~/journal/2026-06-16.org is pre-populated with yesterday's open todos, your calendar, and the Dota match you played last night (from PostGame above).
🎮🐧📝 The Ultimate Personal Stack (combo)
If you wanted one big project that ties all three together:
┌─────────────────────────────────────┐
│        API Gateway (Ocelot)         │
├─────────────────────────────────────┤
│                                     │
│  PostGame (gaming → journal)        │
│  │                                  │
│  ├── gRPC → Stats service           │
│  └── AMQP → Journal service         │
│                                     │
│  JournalCraft (notes → templates)   │
│  │                                  │
│  ├── gRPC → Export service          │
│  └── AMQP → Rollup service          │
│                                     │
│  TileState (TWM → session mgmt)     │
│  │                                  │
│  ├── gRPC → Restore service         │
│  └── AMQP → Auto-save daemon        │
│                                     │
│  DiffThePatch (patch → Org diffs)   │
│  └── AMQP → Digest → published      │
│                                     │
│  Postgres (per-service DBs)         │
│  RabbitMQ                            │
│  Consul (service discovery)          │
│  Docker + K8s                        │
└─────────────────────────────────────┘
Every thing you already do daily — game, tinker with your TWM, write in Org/Neovim — feeds into your own personal microservice ecosystem. That's the kind of project you'd actually want to maintain.
Want me to pick a direction and build a concrete implementation plan?
* TODO can google forms take a file to make me an exam? no, but i can create that app
  [2026-04-17 Fri 21:29]



* EmacsGolf An idea that would actually be interesting

A true EmacsGolf could score solutions in several categories:

Fewest key presses
Fewest interactive commands (M-x counts as one command)
Shortest keyboard macro
Shortest Elisp one-liner
Fastest completion time

That would fit Emacs much better than simply counting physical keystrokes.

Given your recent interest in building study tools and .NET apps, this could actually make for a fun open-source project: an Emacs challenge runner that records commands (this-command), keystrokes, keyboard macros, and Elisp evaluations, then ranks solutions under different scoring rules. I don't know of an existing polished implementation of that.
