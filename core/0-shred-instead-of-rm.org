instead of
=rm -f something=
use

#+begin_src sh
shred -fzvun3 something
    f > force
    z > overwrite with 0's
    v > verbose 
    u > unallocate/delete
    n3 > number of shredding iterations
#+end_src
